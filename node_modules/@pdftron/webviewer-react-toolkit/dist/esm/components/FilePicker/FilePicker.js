import { __assign, __rest } from "tslib";
import classnames from 'classnames';
import React from 'react';
import { EditableText } from '../EditableText';
import { IconButton } from '../IconButton';
import { Icon } from '../Icon';
export var FilePicker = function (_a) {
    var items = _a.items, className = _a.className, props = __rest(_a, ["items", "className"]);
    var filePickerClass = classnames('ui__base ui__filePicker', className);
    return (React.createElement("div", __assign({}, props, { className: filePickerClass }), items.map(function (item) { return (React.createElement("div", { className: classnames('ui__filePicker__file', item.className), key: item.key },
        React.createElement(EditableText, { className: "ui__filePicker__file__text", value: item.name, onSave: item.onRename, locked: !item.onRename }),
        item.onDelete ? (React.createElement(IconButton, { className: "ui__filePicker__file__delete", onClick: item.onDelete },
            React.createElement(Icon, { icon: "Close" }))) : undefined)); })));
};
